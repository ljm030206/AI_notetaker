# Web Framework
fastapi>=0.100.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
aiohttp>=3.9.0

# Audio & Math
# 🚨 가장 중요한 부분: 최신 AI 라이브러리들과의 충돌을 막기 위해 numpy 1.x 버전 강제 유지
numpy<2.0.0 
scipy>=1.11.0
pydub>=0.25.0

# Core ML Stack 
# (M칩 맥에 맞는 최신 버전을 pip가 알아서 찾도록 == 제한을 풀었습니다)
torch
torchaudio
mlx>=0.1.8

# AI infrastructure
tiktoken>=0.4.3
huggingface_hub>=0.18.1
whisperx
# 🚨 화자 분리(Diarization)의 핵심: 2.x는 구형이므로 3.1 이상으로 올려야 정상 작동합니다.
pyannote.audio>=3.1.1 
langchain>=0.1.0
openai>=1.0.0

# Translation & Services
deep_translator>=1.8.1
google-generativeai

# Utilities
websockets>=11.0.0